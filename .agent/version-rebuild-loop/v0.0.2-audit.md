# v0.0.2 Audit

Status: active version packet

## Version

- Version: `v0.0.2`
- Status: `active`
- Turn decision: `incremented-from-v0.0.1`
- Source base: `v0.0.1` update-ready gate plus current `development` worktree

## Source State

- Current repo state: v0.0.2 architecture scaffold plus a deterministic 10.24-kilometer seeded world that embeds the canonical Gold Rush normal-area greybox and feeds terrain, collider, routes, canyon walls, shelves, landmarks, and tile-owned feature placement.
- Current proof state: seeded recipe, active tile selection, collider/continuity, procedural renderer, v0.0.2 integration, production build, exact Pages-route browser proof, and five fixed-profile headless Three.js review captures pass for the terrain slice.
- Current public proof state: deployed public proof was not changed in this local-only slice; the exact Pages base path was refreshed against the final local production artifact and passed.
- Current asset/audio approval state: unchanged; no new asset or audio import occurred.

## Inherited Validated Parts

- Domain service kit contract pattern.
- Runtime registry shape and validation approach.
- Terrain source fixture contracts through material/biome masks.
- Scene-site loader and first-sequence train handoff contracts.
- Sanitizer, report-secret, import-boundary, and build-artifact gates.
- NexusSimulator sanitized output policy.
- Lessons that local proof, public proof, simulator proof, and live multiplayer proof are separate.

## Rejected Parts

- Whole-app inheritance from `v0.0.1`.
- Stale public proof as v0.0.2 proof.
- Helper-only or query-only proof paths as player UX.
- Unapproved candidate assets/audio as runtime content.
- Simulator proof described as live multiplayer proof.
- Renderer-only terrain or object claims as gameplay truth.

## Current Implementation Slice

- Slice: v0.0.2 scalable seeded world/physics terrain-source handoff and normal-area greybox.
- Domain: world / physics / render / goldrush
- Owning kits: `world/terrain-source`, `world/heightfield`, `world/routes`, `world/landmarks`, `physics/terrain-collider`, `render/terrain-renderer`, and `goldrush/desert-map`.
- Data contract: recipe revision `goldrush-seeded-world-r1`, fixed seed `0x47525553`, 10,240x10,240-meter bounds, 128-meter tiles, 80x80 canonical tile grid, four camera-centered render rings at 4/8/16/32-meter steps, a local 4-meter collision window, deterministic tile-owned features, and one world-coordinate height/color source. The origin blends the 180x110 authored basin, two shelves, two physical routes, twelve canyon wall segments, three landmarks, and two blocked boundary areas into that same source.
- Player-facing outcome: the gold field now reads as an open framed basin inside a continuous larger desert; canyon walls frame the space, enlarged mine/town silhouettes anchor the destinations, and a wider terrain-owned route connects them. Player, combat, network, and final-art promotion remain outside this terrain slice.

## Validation Results

- `npm run validate:v002` passed on 2026-07-05 06:48 EDT.
- `npm run validate` passed on 2026-07-05 06:48 EDT.
- `npm run build` passed on 2026-07-05 06:48 EDT, including sanitizer prebuild and public build artifact postbuild gates.
- `npm run proof:public` passed on 2026-07-05 with public smoke screenshots and train boarding evidence.
- v0.0.2 registry proof: 14 domains, 80 kits, 10 grouped proof pages.
- v0.0.2 boundary proof: 5 behavior system controllers, 11 execution kits, 6 render adapters.
- Content proof page passed in browser: title `Content`, summary `4 kits across content`, and the grouped nav now includes `Content`.
- Train smoke checkpoint: public smoke loading-yard state reached `boarding-open`, `playerLockedToTrain: false` before boarding and `handoff-ready`, `playerLockedToTrain: true` after boarding, with `goldrush-train-transition-audio-cues-v1` cue evidence.
- Known limit: Vite still reports the existing large chunk warning; no v0.0.2 failure is attached to that warning.
- 2026-08-28 greybox proof: authored fixture, terrain collider, terrain continuity (80,360 triangles; 0 downward), basin readability, procedural renderer, and `validate:v002` passed; `npm run build` passed; local runtime capture reached `site.gold-field` with no browser console errors.
- Local `npm run check` reaches the environment-owned NexusSimulator discovery gate; all preceding repository validators pass, and direct production build passes.
- 2026-08-28 seeded-world proof: `validate:v002`, domain/runtime/gameplay/import validators through the external NexusSimulator discovery gate, terrain collider, basin readability, procedural renderer, and terrain continuity passed. The active origin snapshot contained 256 tiles split 16/36/68/136 across near/mid/far/horizon rings, 156,864 triangles, 209,664 vertices, and 0 downward faces.
- 2026-08-28 visual proof: five 5-second, 24-FPS, 960x540 Lavapipe captures passed with an identical fixed camera profile. The review loop corrected canyon continuity, wall occlusion, destination scale, and route hierarchy while preserving the seeded terrain contract.
- 2026-08-28 Pages-equivalent proof: the final production build and postbuild sanitizer passed; the exact `/NexusEngine-GoldRush/` preview route passed title-to-run, input-driven mining, input-driven cash-out, route-matrix resolution, and ProtoKit completion.
- 2026-08-28 CI-equivalent gate: `CI=true npm run check` passed all validators, sanitizer gates, production build, and postbuild checks. Without `CI=true`, the same command stops only at the intentionally required external NexusSimulator discovery gate because the sibling simulator checkout is absent.

## Promotion Decision

- Decision: `promote with limits`
- Safe claim: v0.0.2 now has a validated scalable seeded world source shared by world, physics, render, and Gold Rush composition kits, with the authored normal-area greybox embedded at the origin.
- Unsafe claim: v0.0.2 is not the full rebuilt game, not a public-proof-current release, not a gameplay takeover yet, and not a promoted runtime asset pipeline yet.

## Next Turn Recommendation

- Continue `v0.0.2`.
- Next slice: prove tile-boundary streaming, feature retirement/recreation, and collision-window handoff while traversing beyond the authored origin basin.
