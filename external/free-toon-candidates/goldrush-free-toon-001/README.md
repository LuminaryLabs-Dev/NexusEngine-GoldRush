# GoldRush Free Toon Candidates 001

Status: source candidates only

This folder contains a small imported candidate set for the high-fidelity toon-shaded GoldRush direction.

These files are not runtime assets. Do not load them directly from the app until a later approval/promotion pass creates approved `assets/...` records.

Validation:

```bash
node tools/validation/validate-free-toon-candidates.mjs
```
